#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main -Entry point
 * Description: prints the alphabet in lowercase\n
 * Return: Always 0 (success)
 */
int main(void)
	char letter;
	for (letter = 'a'; letter  <= 'z'; letter++)

	{
	Putchar(letter);
	}
	putchar('\n');
	return (0);
