#include <stdio.h>
/**
 * main - Entry point
 * Return: 0
 */
int main(void)
{
int numR;
int numl;
int minl = 48;
for (numR = 48; numR < = 57; numL++)
{
for (numR = minL++; numR < = 57; numR++)
{
if (numR != numL)
{
putchar(numL);
putchar(numR);
if (!(numR == 57 && numL == 56))
{
putchar(',');
puchhar('');
}
}
}
}
putchar('\n');
return (0);
}
