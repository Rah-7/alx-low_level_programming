#include <stdio.h>
#include <unisted.h>
/**
 * main - Entry point
 * Description: prints the alphabet in lowercase\n
 * Return: Always 0 (success)
 */
int main(void)
{
	int ch;

	for (ch = 'a'; ch < = 'z'; ch++)
	{
		putchar (ch)
	}
	putcha('\n');
return (0);
}
