#include <stdio.>
/**
 * main - Entry point
 * Return: 0.
 */
int main(void)
{
char abc;
for (abc = 'a'; abc < = 'z'; abc++)
{
if ((abc != 'e') && (abc != 'q'))
putchar (abc);
}
putcha r('\n');
return (0);
}
